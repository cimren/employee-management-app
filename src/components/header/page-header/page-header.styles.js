import { css } from 'lit';

const styles = css`
  .container {
    max-width: 1440px;
    margin: 0 auto;
    padding: 2rem 1rem;
  }

  .header {
    margin-bottom: 2rem;
  }

  .header h1 {
    color: var(--primary-color);
  }
`;

export default styles;
