import { css } from 'lit';

const styles = css`
  :host {
    display: block;
  }

  input {
    width: 100%;
    padding: 0.5rem;
    border: 1px solid var(--border-color);
    border-radius: 4px;
    font-size: 1rem;
  }
`;

export default styles;
